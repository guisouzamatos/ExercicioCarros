package com.api.exe2.repositories;

import com.api.exe2.models.CarroModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CarroModelRepository extends JpaRepository<CarroModel, Integer> {
}